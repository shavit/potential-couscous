defmodule PathFinder.AStar do
  def path(item) do
    # Find neighbors

    # Find not visited

    # Set next
    item
  end

  def neighbors(item) do
    item
  end

end

queue = []
visited = []

Enum.map(queue, &(PathFinder.AStar.path(&1)))
